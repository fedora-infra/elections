# Used for when working from a virtualenv.
# Use this file by running "$ pip install -r requirements.txt"
flask>=0.10
kitchen
python-fedora
Flask-wtf
python-openid
python-openid-cla
python-openid-teams
